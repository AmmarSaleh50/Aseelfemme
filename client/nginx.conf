server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    # If hosting API elsewhere, proxy_pass here. In this setup API is separate port.
    add_header Access-Control-Allow-Origin *;
  }

  location / {
    try_files $uri /index.html;
  }
}
